<Window x:Class="DownloadMaster.InstagramCookiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Instagram cookies"
        Icon="Assets/app.ico"
        Height="520" Width="640"
        MinHeight="460" MinWidth="560"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HintText" TextWrapping="Wrap" FontSize="13" LineHeight="20"
                   Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,12" />

        <TextBox x:Name="PasteBox" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" MinHeight="180"
                 VerticalContentAlignment="Top" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="ImportFileButton" Content="Import from file..." Style="{StaticResource SecondaryButton}"
                    Margin="0,0,10,0" Click="ImportFileButton_Click" />
            <Button x:Name="CancelButton" Content="Cancel" Style="{StaticResource SecondaryButton}"
                    Margin="0,0,10,0" Click="CancelButton_Click" />
            <Button x:Name="SaveButton" Content="Save and use" Style="{StaticResource PrimaryButton}"
                    Padding="20,10" Click="SaveButton_Click" />
        </StackPanel>
    </Grid>
</Window>
